<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<filter api>
  @type parser
  format json
  key_name log
  reserve_data true
  remove_key_name_field true
  time_format %iso8601
</filter>

<match api>
  @type stdout
</match>
