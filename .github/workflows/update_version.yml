name: Update Version

on:
  repository_dispatch:
    types: [new_api_version, new_spa_version]

jobs:
  update_version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Test
        run: echo 'I got invoked'